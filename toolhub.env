# =========================
# Toolhub Stack Variables
# =========================
# This file is intended for Portainer stack environment variables.
# Toolhub uses one host path mounted to /workspace.

# -------------------------
# Host path base
# -------------------------
# Single host directory mounted to /workspace in the container.
TOOLHUB_PROJECT_DIR=/volume2/docker/toolhub
# Canonical runtime root inside the container.
TOOLHUB_PROJECT_ROOT=/workspace

# -------------------------
# Network and ports
# -------------------------
# External Docker network name used by the stack.
TOOLHUB_DOCKER_NETWORK=allmydocker-net
# Fixed IPv4 address for the toolhub container on TOOLHUB_DOCKER_NETWORK.
TOOLHUB_IPV4_ADDRESS=192.168.123.5
# Host ports mapped to container SSH and webhook ports.
TOOLHUB_SSH_PORT=2222
TOOLHUB_WEBHOOK_PORT=5656

# -------------------------
# Toolhub runtime identity
# -------------------------
# Runtime user configuration applied by start.sh inside the container.
TOOLHUB_USER=toolhubuser
TOOLHUB_PASSWORD=toolhub123
TOOLHUB_UID=1061
TOOLHUB_GID=100

# -------------------------
# Tool dispatch roots
# -------------------------
# Python module import root used by webhook/script wrappers.
TOOLHUB_PYTHON_ROOT=/opt/toolhub
# Manifest tool directory used by webhook /run.
TOOLHUB_MANIFEST_TOOLS_DIR=/opt/toolhub/tools
# Executable script directory scanned by webhook /run.
TOOLHUB_SCRIPT_TOOLS_DIR=/scripts
# Artifact root used by webhook /run-file and /artifacts endpoints.
TOOLHUB_ARTIFACTS_DIR=/shared/artifacts

# -------------------------
# DOCX tool configuration
# -------------------------
# Roots for docx-template-fill tool.
DOCX_TEMPLATE_ROOT=/templates
DOCX_OUTPUT_ROOT=/output
DOCX_TEMPLATE_FILL_LOG_PATH=/logs/docx-template-fill.log
DOCX_TEMPLATE_FILL_LOG_LEVEL=INFO
# Roots for tools.docx_render handler.
DOCX_TEMPLATES_DIR=/data/templates
DOCX_OUTPUT_DIR=/data/output
DOCX_RENDER_LOG=/logs/docx-render.log
DOCX_RENDER_LOG_LEVEL=DEBUG

# -------------------------
# Cleanup tool configuration
# -------------------------
CLEANUP_LOG_PATH=/logs/cleanup.log
CLEANUP_LOG_LEVEL=INFO

# -------------------------
# Transcript tool configuration
# -------------------------
TRANSCRIPT_INPUT_ROOT=/shared/audio/in
TRANSCRIPT_OUTPUT_ROOT=/shared/audio/out/transcripts
TRANSCRIPT_LOG_PATH=/logs/transcript.log
TRANSCRIPT_LOG_LEVEL=INFO

# -------------------------
# Optional MCP settings
# -------------------------
# These are not consumed by toolhub.yaml base service directly, but are useful
# when deploying an MCP sidecar stack in the same Portainer environment.
SAFE_MODE=true
ALLOWLIST_PATHS=/data,/tmp,/shared,/logs,/app
ALLOWLIST_HOSTS=localhost,127.0.0.1
MCP_SCRIPTS_ROOT=/app/scripts
MCP_COMMAND_TIMEOUT_MS=120000
MCP_MAX_OUTPUT=20000
MCP_RATE_LIMIT_COUNT=5
MCP_RATE_LIMIT_WINDOW_MS=10000
NUCLEI_TEMPLATES=/root/nuclei-templates
