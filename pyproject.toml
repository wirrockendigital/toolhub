[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "toolhub"
version = "0.2.5"
description = "Toolhub automation utilities"
requires-python = ">=3.11"
dependencies = [
  "docxtpl>=0.16,<0.17",
  "python-docx>=1.1,<2.0",
]
